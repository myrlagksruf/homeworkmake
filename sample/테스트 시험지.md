# Time20201226

## version : 2

### console.log의 결과로 올바른 것은?

```javascript
let x = 0;
let i = 0;
const a = [5, 3, 2, 5, 7];
const fun1 = () => {
    if(i < 5){
        x += a[i];
     	setTimeout(fun2, 200);   
    } else {
        console.log(x);
    }
    i++;
}
const fun2 = () => {
    if(i < 5){
        x *= a[i];
        setTimeout(fun1, 200);
    } else {
        console.log(x);
    }
    i++;
}
setTimeout(fun1, 200)
/*%{"num":"0","ans":"92"}%*/
```

<input type="text" data-id="0">



```javascript
let i = 0;
let x = 0;
const main = () => {
    if(i % 2 === 0){
     	x += i;
    } else {
        x -= i;
    }
    if(i < 60){
        requestAnimationFrame(main);
    } else {
        console.log(x)
    }
    i++;
}
main();
/*%{"num":"1", "ans":"30"}%*/
```

<input type="text" data-id="1">



```html
<div id="container">
    <table>
        <thead>
            <tr>
                <th>이름</th>
                <th>나이</th>
                <th>성별</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>김한결</td>
                <td>25</td>
                <td>남</td>
            </tr>
            <tr>
                <td>홍길동</td>
                <td>17</td>
                <td>남</td>
            </tr>
            <tr>
                <td>김영희</td>
                <td>21</td>
                <td>여</td>
            </tr>
        </tbody>
    </table>
</div>
<script>
    const arr = [];
    const tbody = document.querySelector('tbody');
    for(let i = 0, tr = tbody.children; i < tr.length; i++){
        let td = tr[i].children;
        setTimeout(() => {
            arr.push(`${td[0].innerHTML},${td[2].innerHTML}`);
            if(arr.length === 3){
                console.log(arr.join(';'));
            }
        }, parseInt(td[1].innerHTML));
    }
</script>
/*%{"num":"2", "ans": "홍길동,남;김영희,여;김한결,남"}%*/
```

<input type="text" data-id="2">



### 다음 div에 나올 색으로 알맞는 것은?

```html
<div id="container"></div>
<script>
    const container = document.querySelector('#container');
    const div = document.createElement('div');
    const date = Date.now();
    const arr = [12, 2, 3];
    const color = [];
    let i = 0;
    let st = setInterval(() => {
        const value = parseInt((Date.now() - date) / 10) * 10;
        if(value / 10 > arr[i]){
            color.push(value);
            i++;
            if(i < 3){
                arr[i] += arr[i - 1];
            } else {
                div.style.backgroundColor = `rgb(${color.join(',')})`;
                div.style.width = '100px';
                div.style.height = '100px';
                container.appendChild(div);
                console.log(div.style.backgroundColor);
                clearInterval(st);
            }
        }
    }, 10);
</script>
/*%{"num":"3", "ans":"#8296b4"}%*/
```

<input type="color" data-id="3">



```html
<!--현재 화면 주사율은 60Hz (오차가 있을 수도 있으니 이론상으로 해주세요)-->
<div id="container">
    
</div>
<script>
    const container = document.querySelector('#container');
    const arr = [2, 0, 1];
    const color = [];
    let i = 0;
    let x = 0;
    const main = () => {
        x++;
        if(i < 3){
            const div = document.createElement('div');
            div.style.backgroundColor = `rgb(${color.join(',')})`;
            div.style.width = '100px';
            div.style.height = '100px';
            container.appendChild(div);
            requestAnimationFrame(main);
        }
    };
    const timeFun = () => {
        i++;
        color.push(x);
        x = 0;
        if(i < 3){
            setTimeout(timeFun, arr[i] * 1000);
        }
    };
    setTimeout(timeFun, arr[i] * 1000);
    main();
</script>
/*%{"num":"4","ans":"#78003c"}%*/
```

<input type="color" data-id="4">



### 다음 그림과 같이 물체가 움직일 때 주석 부분의 코드를 <span style="color:red">띄어쓰기 없이</span> 작성해주세요.

#### (만약 코드의 개수가 다수면 코드 사이에 ';' 를 넣어주세요.)

#### 예) color++;x=x+1;bule<3

<video controls src="./img/5.webm"></video>

```html
<style>
    body{
        height: 100vh;
        margin: 0;
        display: flex;
        justify-content: center;
        align-items: center;
    }
    button{
        font-size:3em;
    }
    div{
        position: absolute;
        background-color: red;
        width: 100px;
        height: 100px;
    }
</style>
<div></div>
<button onclick="this.style.display='none';main();">시작</button>
<script>
    const div = document.querySelector('div');
    const way = /*1번 : 이중배열(1, 0, -1 으로 만들어진)*/;
    let speed = 5;
    let i = 0;
    div.style.left = 0;
    div.style.top = 0;
    const main = () => {
        let x = parseInt(div.style.left) + way[/*2번*/][0] * speed;
        let y = parseInt(div.style.top) + way[/*2번*/][1] * speed;
        if(x < 0 || /*3번*/ > innerWidth){
            i++;
            if(x < 0){
                x = 0;
            } else {
                x = innerWidth - 100;
            }
        }
        if(y < 0 || /*4번*/ > innerHeight){
            i++;
            if(y < 0){
                y = 0;
            } else {
                y = innerHeight - 100;
            }
        }
        div.style.left = `${x}px`;
        div.style.top = `${y}px`;
        requestAnimationFrame(main);
    };
    main();
</script>
/*%{"num":"5", "ans":"[[1,0],[0,1],[-1,0],[0,-1]];i%4;x+100;y+100"}%*/
```

<input type="text" data-id="5" style="width:500px;">

